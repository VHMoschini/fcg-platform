Tech Challenge Fase 2 - Relatorio de entrega (preencher e postar na data)

Nome do grupo:
Participantes e usernames no Discord:
Link da documentacao (README / wiki):
Link do video (YouTube ou outro):

Repositorios Git (um por microsservico, conforme o enunciado):

  Users API          https://github.com/VHMoschini/fcg-users-api
  Catalog API        https://github.com/VHMoschini/fcg-catalog-api
  Payments API       https://github.com/VHMoschini/fcg-payments-api
  Notifications API  https://github.com/VHMoschini/fcg-notifications-api
  Orquestracao       https://github.com/VHMoschini/fcg-platform

  Todos publicos. Cada repo de API traz o codigo, o Dockerfile multi-stage, a
  pasta k8s/ na raiz (Deployment, Service, ConfigMap, Secret), a solucao .sln e
  um README com as variaveis de ambiente. O fcg-platform sobe a stack completa
  com "docker compose up" (ver o README dele: as imagens sao buildadas nos repos
  das APIs antes de subir).

Checklist tecnico (para correcao):

Entregue e conferido:
- [x] Quatro microsservicos .NET 8 em repositorios Git separados (+ orquestracao)
- [x] Dockerfile multi-stage por servico
- [x] k8s/ com Deployment, Service, ConfigMap e Secret na raiz de cada repo
- [x] docker-compose.yml sobe a stack completa (compose validado; imagens fcg/*)

Implementado no codigo, ainda sem execucao end-to-end nesta maquina:
- [x] UserCreatedEvent: Users publica, Notifications consome
- [x] OrderPlacedEvent -> Payments -> PaymentProcessedEvent -> Catalog + Notifications

Pendente de validacao (bloqueado por ambiente - ver abaixo):
- [ ] Rodar "docker compose up" e exercitar os fluxos de cadastro e compra
- [ ] Demo em cluster Kubernetes local (kubectl get pods)

Bloqueio de ambiente: o daemon do Docker Desktop nao consegue baixar as imagens
base do .NET (mcr.microsoft.com responde EOF via proxy interno do Docker), entao
nenhuma imagem foi buildada ainda. O Kubernetes tambem nao esta habilitado no
Docker Desktop. Nada disso depende do codigo - assim que o pull voltar, rodar
build-images.ps1 e depois docker compose up / deploy-k8s.ps1.
